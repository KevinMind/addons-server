This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `app/page.tsx`. The page auto-updates as you edit the file.

This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font), a new font family for Vercel.

## Todo

- [ ] Add waffle switch to control the login page
- [X] Add basic login page to the project
- [ ] Add a swagger generated API client
- [ ] Add a protected route to the project with auth middleware
- [ ] Fetch interesting Users via the api
- [ ] Add prettier and tailwind cleanup to the project
- [ ] Add a package of extracted UI components to the project
- [ ] Add UI page test to the project
- [ ] Add ability to publish the web_frontend image to dockerhub as a part of the build process
- [ ] Add a deployment for production of the web_frontend app and routing to it
- [ ] Add a root URL via environment to set the project base URL
